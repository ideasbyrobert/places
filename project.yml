name: Places
options:
  bundleIdPrefix: com.ideasbyrobert
  createIntermediateGroups: true
  deploymentTarget:
    macOS: "15.0"
configs:
  Debug: debug
  Release: release
# Kept in step with Package.swift by hand: SwiftPM builds the release, the
# generated project builds the tests, and when only one of them knew about
# Sparkle the test target could not compile the app at all.
packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    exactVersion: 2.9.4
settings:
  base:
    ARCHS: "$(ARCHS_STANDARD)"
    CLANG_ENABLE_MODULES: YES
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: X87D35HM5V
    ENABLE_HARDENED_RUNTIME: YES
    MACOSX_DEPLOYMENT_TARGET: "15.0"
    SWIFT_STRICT_CONCURRENCY: complete
    SWIFT_VERSION: "6.0"
targets:
  Places:
    type: application
    platform: macOS
    sources:
      - path: Sources/Places
      - path: Resources/Places.icon
        buildPhase: resources
    info:
      path: .derived/xcodegen/Info.plist
      properties:
        LSApplicationCategoryType: public.app-category.utilities
        LSMultipleInstancesProhibited: true
        LSUIElement: true
        NSAppleEventsUsageDescription: Places uses Terminal Automation only to set the selected tab in each Terminal window to 80 columns by 48 rows when you choose Arrange App Windows 4 × 2.
        SUFeedURL: https://downloads.ideasbyrobert.com/places/appcast.xml
        SUPublicEDKey: +vhVXgERIDkF9jUDCo/eorsxZ8NP52N+ILOpYHZCiKQ=
        SUEnableAutomaticChecks: true
        SUScheduledCheckInterval: 86400
    dependencies:
      - package: Sparkle
        product: Sparkle
    entitlements:
      path: Resources/Places.entitlements
      properties:
        com.apple.security.automation.apple-events: true
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: Places
        COMBINE_HIDPI_IMAGES: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.ideasbyrobert.Places
        PRODUCT_NAME: Places
  WindowFixtureApp:
    type: application
    platform: macOS
    sources:
      - path: Sources/WindowFixtureApp
    info:
      path: Resources/FixtureInfo.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ideasbyrobert.Places.WindowFixtureApp
        PRODUCT_NAME: WindowFixtureApp
  PlacesTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests/PlacesTests
    dependencies:
      - target: Places
      - target: WindowFixtureApp
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_NSAppleEventsUsageDescription: Places tests automate disposable Terminal windows only during the explicit live Terminal test.
        PRODUCT_BUNDLE_IDENTIFIER: com.ideasbyrobert.PlacesTests
  PlacesUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - path: Tests/PlacesUITests
    dependencies:
      - target: Places
      - target: WindowFixtureApp
    settings:
      base:
        ENABLE_HARDENED_RUNTIME: NO
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.ideasbyrobert.PlacesUITests
schemes:
  Places:
    build:
      targets:
        Places: all
        WindowFixtureApp: [test]
        PlacesTests: [test]
        PlacesUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - PlacesTests
        - PlacesUITests
    archive:
      config: Release
  WindowFixtureApp:
    build:
      targets:
        WindowFixtureApp: all
    run:
      config: Debug
